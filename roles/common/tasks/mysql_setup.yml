- name: Installing mysql client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Loading Schema
  ansible.builtin.shell: mysql -h mysql-dev.devopskumar.site -uroot -pRoboShop@1 < /app/schema/shipping.sql
